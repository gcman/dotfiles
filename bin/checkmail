#!/bin/sh

STATE=`nmcli networking connectivity`

if [ $STATE = 'full' ]
then
    mbsync -Va
    /home/gautam/bin/loadmail
    COUNT=`notmuch count tag:inbox and tag:unread`
    if (( $COUNT > 0 ))
       then
           notify-send "$COUNT unread messages"
    fi
    exit 0
fi
echo "No internet connection."
exit 0
